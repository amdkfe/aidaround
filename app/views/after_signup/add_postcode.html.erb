<p>Please Fill in your post code!</p>
<%= form_for(@user, :url => wizard_path, :method => :put) do |f| %>
  <%= f.text_field :postcode, :placeholder => "post code code" %>
  <%= f.submit 'Next', :class => 'btn btn-primary' %>
<% end %>

<%= link_to 'skip', next_wizard_path %>